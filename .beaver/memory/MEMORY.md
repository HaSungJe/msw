# Beaver Memory Index

- [workflow](workflow.md) — .info/는 로컬 전용·커밋 금지·개발 시 필독(09-22) / .info 형제 문서(character·skill(스킬 툴팁 원본, 09-22 분리 — 아이콘은 안 씀)·balance-detail·damage·augmentation·weapon) / 로드맵·플랜은 코드 없이 방향 설명 / 페이즈 완료는 사용자 만족 루프 / 커밋·푸시는 매번 명시 승인 / 커밋 메시지 = 날짜 + `* ` 불릿·트레일러 금지 / character.md ↔ balance-detail.md ↔ damage.md ↔ augmentation.md 형제(계산식은 damage.md, 증강 정의는 augmentation.md — 사용자가 채움) / 화면 목업 원본은 .info/artifacts/hud-layout.html / 스킬 연출은 좌우 방향(assetFacesLeft·noFlip·요소별 FacesLeft) 썸네일 시트 + 허수아비 3기 시험대 + 슬로모 미리보기로 확인 후 올린다
- [msw-engine](msw-engine.md) — 개발모드 = 주니어 발록 보스 1기·영입 팝업 자유 영입(Lv50)·우측 하단 테스트 프리즘(토글)·보스 라운드 취급, 프리즘 = Prisms + GetSkillsFor/GetStatFor 오버레이(2026-09-22) / 프롭은 model://MapObject / 유닛은 아바타 방식(AddComponent) / 애니 끝은 EndFrameEvent / 줌 잠금 시 ZoomTo 무시 / PPU 30·Truncate / 공식 자산 검색 절차 / number tostring은 "1.0" → Int 포맷 / 터치·커서·UI 좌표·RPC(userId 마지막 인자, senderUserId) / 아바타 기본 왼쪽 보기 → FaceDir + 루트 스케일 x 반전 / 클라 스폰은 프레임 분산, MCP 도구 유령 클릭 / 스크롤 목록 = ScrollLayoutGroup(프로퍼티는 ScrollBar*, 짧으면 UseScroll 끔) / 월드 클릭은 ScreenTouchEvent + 자체 상자 판정 한 곳(엔티티 TouchEvent 폐기 09-22), UI 우선은 IsPointerOverUI·메뉴 사각형 / 전투 = 엔진 Attack/Hit/데미지 스킨 위에(RtsUnitAttackComponent 확장, AttackFrom + IsAttackTarget 이름 필터, 시험대 RtsCombatLogic.TestBench) / 데미지 스킨 = 기본 (유닛) 208번 7e39645a…(만/억 자동 포맷, 캐시→DXT5 디코드로 확인) / execute_script 서버는 context=server_main / Maker UI 자동화 금지 / 원작 클립 프레임 원점 = (ox, oy 바닥기준) px·프레임 시간은 .win.mod에서 읽음 / 슬로모 미리보기(PlayRate 0.05) / 흰 사각 바는 Scale로(TiledSize 무효), 자식은 부모 스케일 나눔 / 같은 아바타 액션은 stand 경유 없이 재생 안 됨·ActionStateChangedEvent playRate로 모션 늘리기 / 이펙트에 무기가 그려진 스킬은 hideWeapon(SetAvatarPartColor alpha 0)
- [assets](assets.md) — 몬스터 = 이동·사망 클립 + 사망음(달팽이 RUID) / 속도 100 = 3.5u/s / 히어로·팔라딘·다크나이트 모험가 세트와 스킬 자산 RUID(히어로 1레벨 = 레이징 블로우 1121008/effect/2 + hit음, 2026-09-18 교체; 프리즘 강화판 1120017은 타격음만 있음) / 나머지 8직업 세트(캐시 무기 제외, 메이플 시리즈 진짜 무기·1H 슬롯·모자·마스크 — GetCostume 단일 소스) / 마법사 3직업 스킬 자산(썬더볼트 hit·아이스 스트라이크 기둥·2221012 결정·포이즌 미스트 tile·플레임 위자드 불덩이·제네시스 tile, 빅뱅 이전 클립은 없음) / 발판 틀·손 커서·파티 버프 스킬 아이콘 4종 / 테마 프리셋
- [balance](balance.md) — 프리즘 자산 선별 완료(09-22): 11직업 전부 통과(팬텀 연출 없음) → 순서 확정: 6 웨이브 → 7 보스 → 5 유닛 관리 → 4 증강 → 8 방/멀티(+로비 전광판 Top 10~100: 최대 클리어 라운드 → 보스 HP 깎은 비율) → 10 테마(유료)·아바타 프리셋(영구)·아이콘 컬렉션(처치 시 일반 1%·보스 5%, 기본 초록달팽이)·좌측 플레이어 카드(아이콘·테마·닉네임·내 카드만 수정 버튼, 서버 저장) → 9 밸런싱 / 영입 = 직업 선택 후 발판 클릭 배치, ESC = 영입 취소(09-22) / 방출 = 증강 소멸·레벨업 메소 30% 환급(캐릭터 메뉴 → 확인 팝업) / 증강 3택1 = 3장 서로 다름·브실골 중복 획득 허용·프리즘 1회·후보 11직업 전부+팬텀·대상은 증강창에서 1회 지정(Phase 4 spec 2026-09-22) / 썬콜 프리즘 2개(엘릭서 + 블리자드 템페스트(가칭): 보스전 15타 집중·보스 최종 ×0.9 → 최고점 나로 ×1.31, 2026-09-22) / 공격 사이클 배속 규칙 / 사거리·타겟 규칙 / 데미지 계산 순서(합·곱·뺄셈) / 액티브 1개 규칙 / 대상 지정 = 셀렉트 박스 / 대상 변경·위치 이동 쿨 = 유닛당 3분 고정 / 과금 = 월드 아바타 30일 1,600코인(상품 QK03ZI15E) / 히어로 대기만성·팔라딘 초반 캐리 곡선 / 메소 30만 예산·비용 곡선 / 파티 버프 4종 = 발 아래 아이콘 + 팝업 우측 탭(스킬/증강/버프, GetStatRaw엔 안 넣음) / 증강 규칙 = 라운드별 등급, 총 32개(브10·실10·골7·프5 — 2026-09-22)·전부 합연산·중복 허용·보스 미처치 = 탈락 / 증강 점검 가정·발견(1회당 합연산, 골드 쿨 2초 보스 손해 / 크리 증강 배치·프리즘 스킬 잠금은 의도된 설계 — 재제기 금지) / 프레이 자신 미적용 / 레이징 블로우 50%×2회 / 액티브 스킬 잠금 설정 [사용/보스전 잠금/잠금 + 영구 잠금 3 = 프리즘 강제·해제 불가](SkillLocks 문자열, 로어 기본 보스전 잠금, 쿨 없음) / 팝업엔 내부 설계 수치(누적 투자·벽·보유 메소) 노출 금지 — 하단은 레벨업 버튼만 / 공격 = 메인 대상 1마리·그쪽으로 좌우 반전·타격마다 따로 판정(데미지 스킨 = 메이플 기본 (유닛) 7e39645a…, 2.0 Blade) / 타겟 선택 = 체비셰프 사거리·가까운 순 n마리, 쿨 스킬은 대상 있을 때만 / 후딜 1.5초 4스킬 / 빙결 틴트+아이콘 / dot·spread(도트 퍼니셔만 보스 우선) / 체력바 1.4 통일·보스 2.6 / **다음 작업(2026-09-20 저녁): 로드맵 재정리 완료 — 09-21 사용자가 프리즘 11종 정의 마무리 → `/beaver:direct 증강·프리즘`(Phase 4)부터 로드맵 사이클, 그다음 유닛 관리 → 스테이지·웨이브(몬스터 RUID 매칭)** / 빙결 = 5초·이동 ×0.5·머리 위 결정체 아이콘(눈꽃 대체, 세계 0.7) / 광역 4종 몬스터 위 타격 클립 / 타 유저 구역 보기 = 읽기 전용(영입 패널엔 직업·레벨만, 상세보기만 활성, 조작 전부 잠금)
