# Roadmap — 벽짓살 MSW판 (탑다운 풀 RTS 디펜스: 벽으로 막고, 경제 키우고, 제한시간 내 보스 처치)

## Direction
스타크래프트2 유즈맵 '벽 짓고 살아남기'를 MSW 탑다운 풀 RTS로 재현한다. 플레이어는 일꾼으로 벽·수익건물·포탑·병영·업그레이드센터를 지어 상시 스폰되는 몹을 막고 경제를 키운 뒤, 제한시간 내에 맵 중앙의 보스 건물을 부수고 각성한 보스를 처치하면 승리한다. 방 생성→입장→시작 구조로 멀티/솔로 모두 지원하며, 일꾼이 죽은 유저는 개인 탈락(비석)한다. (방향은 느슨하게 유지 — 개발 중 변경 가능)

## Scope & Actors
- **플레이어** — 일꾼·전투유닛을 RTS 조작(클릭/드래그 선택, 부대지정 Ctrl+숫자, 단축키, 카메라 스크롤)으로 지휘. 건설/업그레이드/전투 판단 주체.
- **방장** — 방 생성, 난이도 선택, 게임 시작.
- **시스템(서버)** — 몹 스폰·AI·강화 스케일링, 보스 각성·추격, 경제 틱, 승패 판정.
- **커스터마이즈 축** — 난이도(몹 공격력/공속/체력/방어력 %스케일), 방 인원(솔로~멀티).

## Non-Goals / External Boundaries
- **PvP 없음** — 순수 PvE 협동/개인 생존.
- **전장의 안개(시야) 1차 미도입** — MSW 네이티브 미지원, 필요 시 이후 신규 유닛으로.
- **모바일 최적화 1차 제외** — 풀 RTS 조작(키보드+마우스)은 PC 중심. 모바일 대응 범위는 Open Decision.
- **운빨 디펜스 컨셉 폐기** — 랜덤 뽑기/조합 요소 없음.
- 메이플 IP 리소스 사용은 MSW 정책 범위 내에서만.

## Phases & Units

### Phase 1 — RTS 기반 (탑다운 조작·그리드 토대가 서야 모든 게 올라간다)
| # | Unit | Goal | Key decision | Depends | Status |
|---|------|------|--------------|---------|--------|
| 1 | 탑다운 맵·카메라 리그 | 중력 없는 탑다운 맵 + 카메라를 아바타에서 분리해 키/엣지 스크롤(`GetCursorPosition()` 폴링) + 창모드 커서 가두기(`CursorLockMode.Confined`, PC 전용). 아바타 숨김 처리 | 맵 크기·셀 크기(#6 길찾기 성능 실측 후 상한 확정 — 초기값으로 시작, 맵 영역은 `MapComponent.LeftBottom/RightTop`으로 가변) | — | done |
| 14 | HUD·UI v1 (스타식 셸) | **[사용자 지정: #1 바로 다음]** 스타식 레이아웃 골격: 미니맵(좌하단 — 지형 + 카메라 뷰포트 표시 + 클릭 시 카메라 점프), 커맨드 카드 틀(우하단 버튼 슬롯), 선택 정보 패널 자리(중앙 하단), 상단 자원/시간 표시 자리. 유닛/건물 도트와 실버튼 연결은 #3·#4·#9에서 채움 | 미니맵 렌더 방식(월드 축소 캡처 vs 도트/색상 타일) | 1 | in-progress |
| 2 | 논리 그리드 시스템 | 그리드 점유 관리(지형벽=불가침 셀, 건물=점유 셀), 배치 가능 검증 단일 소스 | 그리드 데이터 구조(크기·청크·동기화 범위) | 1 | todo |
| 3 | RTS 선택·명령 입력 | 클릭/드래그 박스 선택, 부대지정(Ctrl+숫자→숫자 호출), 단축키, 명령 디스패처 | 이동 명령 입력 방식(우클릭 인식 가능 여부 → 대체 조작) | 1 | todo |

**Expected scenes/systems** (게임 포지션 — 이름은 가안, 각 유닛 기획(spec)에서 확정 시 갱신):

| Unit | Scene·System | Trigger | Purpose |
|------|--------------|---------|---------|
| #1 | `RtsMap` (맵) + `CameraRigLogic` | 게임 입장 | 탑다운 필드·카메라 스크롤 |
| #2 | `GridLogic` | 건설/파괴/길찾기 호출 | 셀 점유·배치 검증의 단일 소스 |
| #3 | `SelectionLogic` + `CommandLogic` | 마우스/키보드 입력 | 선택 상태 관리·명령 전달 |

### Phase 2 — 코어 루프 (혼자서 한 판이 처음부터 끝까지 돌아간다)
| # | Unit | Goal | Key decision | Depends | Status |
|---|------|------|--------------|---------|--------|
| 4 | 일꾼·건설 | 일꾼 유닛 소환·조작(플레이어의 분신 — 서버 스폰 동기화 엔티티라 **모든 유저에게 보임**; 숨긴 아바타는 카메라 앵커일 뿐), 건설 메뉴(단축키+클릭), 그리드 스냅 설치/철거, 자원 차감 | 건설 시간(즉시 vs 소요시간) | 2, 3 | todo |
| 5 | 경제(수익건물) | 자원 보유·표시, 수익건물 설치/레벨업(L1 300→수입20, L2 400→40 … 가안), 설치 한도 20→확장 업글(500원/2분/+5, 최대 60), 몹 처치 보상 훅 | 밸런스 테이블 초안 확정 | 4 | todo |
| 6 | 길찾기(흐름장+A*) | 몹용 유저별 공유 흐름장(BFS, 플레이어 벽=고비용, 지형=불가) — 입구 뚫리면 진입·막히면 벽 공격이 자연 유도. 유닛용 개별 A* | 재계산 트리거·비용 함수(벽 HP 반영 여부) | 2 | todo |
| 7 | 몹 스폰·AI | 상시 스폰(스폰 지점·주기), 타깃 선정(가까운 생존 유저), 흐름장 추종, 경로상 벽 공격, 시간 경과 %강화 | 스폰 곡선·강화 주기 수치 | 6 | todo |
| 8 | 전투·체력 | HP/데미지 공식(공격력·공속·방어력), 포탑 자동 공격, 벽·건물 파괴(그리드 해제→흐름장 갱신), 일꾼 피격·사망 | 데미지 공식(방어력 처리 방식) | 4, 7 | todo |
| 9 | 병영·전투유닛 | 병영 생산 큐, 전투유닛 이동(A*)·자동 전투, 부대지정 연동 | 유닛 종류 수(1차 1~2종 권장) | 3, 6, 8 | todo |
| 10 | 업그레이드 센터 | 공통 강화 연구(공격력 등, 소요시간制), 연구 효과의 전투 반영 | 연구 트리 범위(1차 최소 세트) | 5, 8 | todo |
| 11 | 보스전·게임 종료 | 중앙 보스(**마왕 발록**) 건물→파괴 시 각성(유저 추격, 벽 수리 무효, 벽 관통급 공격), 제한시간, 승리(보스 처치)/패배(시간 초과·전원 탈락) 판정, 비석·관전, 결과 화면 | 보스 스펙·제한시간 값 | 8 | todo |

**Expected scenes/systems**:

| Unit | Scene·System | Trigger | Purpose |
|------|--------------|---------|---------|
| #4 | `WorkerComponent` + `BuildLogic` | 건설 단축키/클릭 | 일꾼 조작·건물 설치 |
| #5 | `EconomyLogic` + `IncomeBuildingComponent` | 수입 틱/처치 이벤트 | 자원 수급·수익건물 레벨 |
| #6 | `PathfindLogic` (FlowField + AStar) | 그리드 변경/이동 명령 | 몹·유닛 경로 |
| #7 | `MobSpawnerLogic` + `MobComponent` | 게임 시작 후 상시 | 몹 스폰·추적·강화 |
| #8 | `CombatLogic` + `TurretComponent` | 사거리 내 적 감지 | 자동 전투·파괴 처리 |
| #9 | `BarracksComponent` + `CombatUnitComponent` | 생산 명령 | 전투유닛 생산·지휘 |
| #10 | `UpgradeCenterComponent` | 연구 명령 | 공통 강화 |
| #11 | `BossComponent` + `GameFlowLogic` | 보스 건물 파괴/타이머 | 각성 보스·승패 판정 |

### Phase 3 — 멀티·운영 (여럿이 함께, 반복 플레이가 즐거워진다)
| # | Unit | Goal | Key decision | Depends | Status |
|---|------|------|--------------|---------|--------|
| 12 | 방 시스템 | 방 생성/입장/시작(솔로 시작 허용), 게임 세션 격리 | MSW 인스턴스 맵 구조(방=인스턴스 맵 여부) | 11 | todo |
| 13 | 멀티 동기화·권위 검증 | 유저별 자원/탈락/부대지정 분리, 서버 권위 재점검, 동시 플레이 검증 | 유저별 상태의 동기화 범위(전체 vs 소유자만) | 12 | todo |
| 15 | 난이도·밸런싱 | 방 설정에서 난이도 선택(%스케일 적용), 전체 수치 튜닝 패스 | 난이도 단계 수·배율 | 13 | todo |
| 16 | 테마·아트 | 개미굴·발록 테마 적용(배경 교체 — 현재 산악 패럴랙스는 미스매치, 수익건물 테마, 메이플 리소스·사운드) | 수익건물·몹·보스 테마 | 11 | todo |

**Expected scenes/systems**:

| Unit | Scene·System | Trigger | Purpose |
|------|--------------|---------|---------|
| #12 | `LobbyMap` + `RoomLogic` | 월드 입장 | 방 생성·입장·시작 |
| #13 | (기존 시스템 검증) | 멀티 세션 | 권위·동기화 보증 |
| #14 | `HudGroup` (UI) | 게임 중 상시 | 정보 표시 |
| #15 | `DifficultyLogic` | 방 설정 | 난이도 스케일 |
| #16 | (리소스 교체) | — | 테마 적용 |

**Progress**: 1/16 done
**Next up**: #14 HUD·UI v1 (스타식 셸) — 사용자 지정 순서, 의존(#1) 충족. `/beaver:direct 스타식 HUD 셸`로 시작. (근거: .beaver/output/report/rts-base/topdown-camera-rig-report.md)

## Cross-Cutting Rules
- **모든 판정은 서버** — 자원 차감, 데미지, 사망, 승패, 몹 스폰은 `@ExecSpace("Server")`. 클라이언트는 입력·표시만 (WorldConfig AuthorityCheck 활성 상태).
- **좌표는 논리 그리드 경유** — 배치·점유·길찾기는 전부 #2 GridLogic 좌표계를 단일 소스로 사용. 월드 좌표 직접 계산 금지.
- **밸런스 수치는 테이블로 일원화** — 비용·수입·HP·데미지·스폰 곡선을 코드에 하드코딩하지 않고 한 곳(데이터 테이블 Logic)에 모은다. 난이도 %스케일(#15)이 이 테이블 위에 곱해진다.
- **키보드 입력은 PC 전용** — 모든 키 조작은 InputService `KeyDownEvent`(Client) 기반, 조작마다 모바일 대체 수단 여부는 Open Decision 결정을 따른다.
- **몹 행동 원칙** — 길찾기 비용이 행동을 결정한다: 입구가 뚫려 있으면 진입, 막혀 있으면 최저비용 경로상의 벽을 공격. 지형벽은 파괴·통과 불가.

## Open Decisions (roadmap-level)
- [ ] **이동 명령 입력 방식** — MSW에서 우클릭 구분 인식 가능한지 확인 → 불가 시 대체(어택땅 키+클릭 등). #3 착수 전 확정 (#3, #9 영향)
- [ ] **모바일 지원 범위** — PC 전용으로 갈지, 최소 버튼 UI를 제공할지 (#3, #14 영향)
- [ ] **밸런스 수치 확정** — 경제 테이블(사용자 제시 수치는 가안)·몹 스폰/강화 곡선·보스 스펙·제한시간 (#5, #7, #11, #15 영향)
- [x] **월드 테마 확정 (2026-08-02)** — 보스 = **마왕 발록**, 배경 = **개미굴**(어두운 흙/갱도). 바닥 타일 개미굴풍으로 교체 진행. 수익건물 세부 테마(버섯 농장 등)는 #16에서.
- [ ] **전장의 안개** — 기본 미도입. 도입 원하면 신규 유닛으로 추가 (전 유닛 영향이라 늦게 뒤집으면 비쌈)

## Notes / Risks
- **#1에서 확보된 직렬화 스키마**: 타일셋 `datas[{Id=스프라이트RUID, Name, IsCollidable}]` / 맵 타일 `tileMap[{type, position, tileIndex(0-based)}]` / 런타임 `BoxFill`은 1-based 인덱스. GridSize 4×4유닛(64×64타일). SpriteRenderer 타일드 바닥은 맵 구석 렌더링 문제로 금지 — 바닥은 반드시 RectTileMap 사용 (#2 그리드·#16 테마에 적용).
- **대량 타일 조작 주의**: BoxFill 65k(GridSize 1)는 에디터 3분 프리즈. 타일 수는 수천 단위 유지.
- **길찾기(#6)가 최대 기술 리스크** — Lua로 그리드 BFS/A*를 돌리는 성능 검증을 #6에서 조기에 실측할 것 (맵 크기·재계산 주기가 여기서 결정됨).
- **풀 RTS 입력(#3)은 MSW 비정형 영역** — 드래그 박스·부대지정은 네이티브 지원이 없어 전부 커스텀. 우클릭 이슈 포함 #3에서 프로토타입 먼저.
- 브레인스토밍 완료·설계 승인 이력: 흐름장 길찾기 방식(A안 수정판)까지 사용자 합의됨 (2026-07-31). 상세 스펙은 각 유닛의 direct 기획에서 작성.
- Maker MCP는 에디터 실행 중에만 연결됨 — 유닛 검증(Play Test) 시 Maker 실행 필요.
